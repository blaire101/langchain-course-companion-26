# Must-Do Exercises

1. Change the ticket classification schema to include `customer_sentiment`.
2. Ask two related questions and explain what information must be stored as memory.
3. Add a translation step after a summary step to create a two-stage chain.
4. Change chunk size from 400 to 150 and compare retrieval results.
5. Add one unanswerable question to the evaluation set and require an explicit insufficient-evidence response.
6. Add a `calculate_discounted_cost` tool and verify that the agent selects it only for pricing calculations.
